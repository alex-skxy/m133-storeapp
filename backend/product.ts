class Product {
    constructor(public id: string, public productName: string, public specialOffer: number, public normalPrice: number, public imageName: string, public description: string){
    }
}

export default Product;